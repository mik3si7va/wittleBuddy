#pragma once

// Planned responsibility: DC motor control and wheel encoder acquisition; motor drivers, encoders and pins are TBD.
// Placeholder only: these methods currently perform no hardware operations.
class DriveSystem {
public:
    void begin();
    void update();
};
