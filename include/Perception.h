#pragma once

// Planned responsibility: Ultrasonic, ToF and 360-degree LiDAR sensing; devices and interfaces are TBD.
// Placeholder only: these methods currently perform no hardware operations.
class Perception {
public:
    void begin();
    void update();
};
