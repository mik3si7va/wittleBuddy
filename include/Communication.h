#pragma once

// Planned responsibility: Higher-level controller communication, including a possible ESP32; transport and protocol are TBD.
// Placeholder only: these methods currently perform no hardware operations.
class Communication {
public:
    void begin();
    void update();
};
