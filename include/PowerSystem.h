#pragma once

// Planned responsibility: Battery voltage monitoring and power state information; battery and measurement circuit are TBD.
// Placeholder only: these methods currently perform no hardware operations.
class PowerSystem {
public:
    void begin();
    void update();
};
