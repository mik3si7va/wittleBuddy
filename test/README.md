# Tests

Reserved for PlatformIO tests. No automated tests are implemented yet. Initial validation passed: the Mega firmware builds, AVRDUDE wrote and verified all 1,856 bytes, and the expected startup message was received after reset at 115200 baud. These were build and manual hardware checks, not automated tests. Add meaningful tests as subsystem behaviour is defined.
